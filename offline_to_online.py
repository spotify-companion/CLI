import sys
from spotify_client import SpotifyClient
import os

if __name__ == "__main__":
    # client = SpotifyClient()
    name = str(input("Give a name for the playlist to be made"))
    directory = str(input("Mention the directory with the songs to be added. Give the full path "))
    # client.make_playlist_with_dir(directory, name)


